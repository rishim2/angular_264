//global jasmine function
describe("Limited Coin Transaction Page", function () {
    it("do calcuation", function () {
        /*describe('routing', function () {
            it('should map routes to controllers', function () {

                beforeEach(module('vmApp'));

                inject(function ($route) {
                    expect($route.routes['/limitedCoinTransactionPage'].controller).toBe('vmLimitedPageController');

                    expect($route.routes['/limitedCoinTransactionPage'].templateUrl).
                    toEqual('limitedCoinTransaction.html');

                    expect($route.routes['/unlimitedCoinTransactionPage'].templateUrl).
                    toEqual('unlimitedCoinTransaction.html');

                    expect($route.routes['/unlimitedCoinTransactionPage'].controller).
                    toEqual('vmUnlimitedPageController');

                    // otherwise redirect to
                    expect($route.routes[null].redirectTo).toEqual('/limitedCoinTransactionPage')
                });
            });

        });*/

    });
});